---
title: How to Set Up CI/CD with GitHub Actions
date: '2026-03-14'
tags: ['github', 'ci-cd', 'automation', 'devops']
draft: false
category: Tech
summary: Learn how to automate your build, test, and deployment pipeline using GitHub Actions. A step-by-step guide for developers who want to ship code faster and safer.
---

## Introduction

Every time you push code, you want to be completely sure it doesn't break anything. **CI/CD** (Continuous Integration / Continuous Deployment) automates this exact process—running tests, building the app, and deploying it automatically.

<Callout type="info" title="GitHub Actions">
  GitHub Actions is GitHub's built-in CI/CD platform. It's incredibly powerful, easy to configure using YAML files, and completely free for public repositories!
</Callout>

---

## Creating Your First Workflow

GitHub Actions are driven by **workflow files** placed inside `.github/workflows/`. 

<Stepper>
  <Step title="Create the File">
    Create a new file: `.github/workflows/ci.yml`
  </Step>
  <Step title="Define Triggers">
    Specify when the workflow should run. For example, on pushes and pull requests to the `main` branch.
  </Step>
  <Step title="Define Jobs & Steps">
    Write the actual steps to checkout code, install dependencies, run tests, and build.
  </Step>
</Stepper>

### The Workflow YAML

Here is a complete example for a Node.js project:

```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run tests
        run: npm test

      - name: Build project
        run: npm run build
```

---

## Best Practices

<Callout type="warning" title="Secure Your Tokens!">
  Never hardcode API keys or deployment tokens in your YAML files! Always use GitHub Secrets and reference them like this: `${{ secrets.VERCEL_TOKEN }}`.
</Callout>

- **Cache Dependencies:** Notice the `cache: 'npm'` line in the workflow? This speeds up your runs dramatically by caching `node_modules`!
- **Run on every PR:** Catch bugs *before* they get merged into the main branch.
- **Keep it modular:** If your workflow gets too large, split CI (testing/building) and CD (deploying) into separate YAML files.

Push this file, click the **Actions** tab in your repo, and watch your automated pipeline come to life!
