---
title: REST API vs GraphQL — Which Should You Use?
date: '2026-03-14'
tags: ['api', 'graphql', 'rest', 'backend']
draft: false
category: Tech
summary: REST and GraphQL are two popular approaches to building APIs. Learn the key differences, pros and cons, and how to choose the right one for your project.
---

<Flashcard term="REST" description="Representational State Transfer — Traditional API style using multiple endpoints with fixed responses. Uses standard HTTP verbs (GET, POST, PUT, DELETE)." category="API Architecture" />
<Flashcard term="GraphQL" description="Query language for APIs — Single endpoint where clients specify exactly what data they need. Developed by Facebook in 2012." category="API Architecture" />
<Flashcard term="Over-fetching" description="Getting more data than needed from an endpoint. Common in REST when an endpoint returns 50 fields but you only need 2." category="API Performance" />
<Flashcard term="N+1 Problem" description="Making N+1 requests to fetch nested data. In REST: fetch user (1), then their posts (N), then each post's comments (N×M)." category="API Performance" />

Every web app needs a way for the frontend and backend to talk. You've got two main options: **REST** and **GraphQL**.

Which one should you use?

---

## The Contenders

<Stepper>
  <Step title="REST (Representational State Transfer)">
    Standard HTTP methods (`GET`, `POST`, `PUT`, `DELETE`) across multiple endpoints. Each endpoint returns a server-defined data structure — you get what they give you.
  </Step>
  <Step title="GraphQL">
    A query language with a **single endpoint**. The client asks for exactly the data it needs — no more, no less.
  </Step>
</Stepper>

---

## The Key Differences

<Callout type="warning" title="1. Data Fetching (Over-fetching vs. Precision)">
**REST** returns whatever the server sends. Hit `/users/1` and you might get 50 fields back when all you needed was the user's `name`.
**GraphQL** lets you pick the fields you want. The response matches your query, nothing extra.
</Callout>

<ConceptCompare
  columns={["Aspect", "REST", "GraphQL"]}
  rows={[
    { feature: "Endpoints", cells: [{ text: "Multiple (e.g., /users, /posts)", type: "neutral" }, { text: "Single (/graphql)", type: "positive" }] },
    { feature: "Response Shape", cells: [{ text: "Server-defined (fixed)", type: "negative" }, { text: "Client-defined (flexible)", type: "positive" }] },
    { feature: "Over-fetching", cells: [{ text: "Common — get all fields", type: "negative" }, { text: "Impossible — select only needed", type: "positive" }] },
    { feature: "Under-fetching", cells: [{ text: "May need multiple requests", type: "negative" }, { text: "Solved — nested queries", type: "positive" }] }
  ]}
/>

<Callout type="danger" title="2. The N+1 Problem (Number of Requests)">
Need a user, their posts, and their followers? With **REST** that's three separate requests. With **GraphQL** it's one.
</Callout>

<ConceptCompare
  columns={["Scenario", "REST Requests", "GraphQL Requests"]}
  rows={[
    { feature: "User + Posts + Comments", cells: [{ text: "3+ requests", type: "negative" }, { text: "1 request", type: "positive" }] },
    { feature: "Dashboard with 10 widgets", cells: [{ text: "10+ requests", type: "negative" }, { text: "1 request", type: "positive" }] },
    { feature: "Mobile API (limited bandwidth)", cells: [{ text: "Heavy payloads", type: "negative" }, { text: "Minimal payloads", type: "positive" }] }
  ]}
/>

<Callout type="info" title="3. Versioning">
**REST** APIs manage versioning through URLs (`/api/v1/` vs `/api/v2/`). **GraphQL** sidesteps this — add new fields, deprecate old ones, no version bumps.
</Callout>

---

## Architecture Comparison

<FlowDiagram direction="horizontal"
  nodes={[
    { id: "client", label: "Frontend", color: "primary" },
    { id: "rest", label: "REST API\nMultiple Endpoints", color: "warning" },
    { id: "graphql", label: "GraphQL\nSingle Endpoint", color: "success" },
    { id: "db", label: "Database", color: "default" }
  ]}
  edges={[
    { from: "client", to: "rest" },
    { from: "rest", to: "db" },
    { from: "client", to: "graphql" },
    { from: "graphql", to: "db" }
  ]}
/>

---

## Which Should You Choose?

### When to choose REST

<ConceptCompare
  columns={["Factor", "REST Advantage"]}
  rows={[
    { feature: "Simple API", cells: [{ text: "Faster to set up, less tooling", type: "positive" }] },
    { feature: "HTTP Caching", cells: [{ text: "Native browser/CDN caching works out of the box", type: "positive" }] },
    { feature: "Public/Third-party APIs", cells: [{ text: "Well-understood, easy for external developers", type: "positive" }] },
    { feature: "Team Experience", cells: [{ text: "Most backend devs know REST well", type: "positive" }] }
  ]}
/>

### When to choose GraphQL

<ConceptCompare
  columns={["Factor", "GraphQL Advantage"]}
  rows={[
    { feature: "Complex Data Model", cells: [{ text: "Clean nested queries instead of JOIN chaos", type: "positive" }] },
    { feature: "Multiple Clients", cells: [{ text: "Each client (Web/iOS/Android) gets exactly what it needs", type: "positive" }] },
    { feature: "Performance Critical", cells: [{ text: "Eliminate over-fetching, reduce payload sizes", type: "positive" }] },
    { feature: "Evolving Schema", cells: [{ text: "Add fields without versioning, deprecate gracefully", type: "positive" }] }
  ]}
/>

<Quiz
  question="You're building a public API for thousands of external developers. Which approach is generally easier for them to adopt?"
  options={[
    "GraphQL — single endpoint is simpler",
    "REST — familiar patterns, standard tooling, HTTP caching",
    "Both are equally easy",
    "Depends on the client language"
  ]}
  correctIndex={1}
  explanation="REST's biggest advantage is that developers already know it. HTTP verbs, status codes, Postman, curl — it's all standard stuff. GraphQL means learning a query language and a new set of client libraries."
/>

<Quiz
  question="Your iOS app needs only user.name and user.avatar, but your web app needs the full user profile with posts and followers. How does GraphQL help?"
  options={[
    "Creates separate endpoints for each client",
    "Each client queries exactly the fields it needs from one endpoint",
    "Automatically detects client type and returns different data",
    "Uses different GraphQL schemas per client"
  ]}
  correctIndex={1}
  explanation="GraphQL's single endpoint handles every client. The iOS app queries `{ user { name avatar } }` while the web app queries `{ user { name avatar posts { title } followers { name } } }`. Same endpoint, zero server-side changes."
/>

<Callout type="idea" title="The Verdict">
For most small to medium projects, **REST works fine** and you'll ship faster. If you're building a complex app with multiple clients, **GraphQL is worth the extra setup.**
</Callout>

<Badge variant="primary">API Design</Badge> <Badge variant="secondary">GraphQL</Badge> <Badge variant="outline">REST</Badge> <Badge variant="outline">Architecture</Badge>
