---
title: "Integrating Google Calendar as an Agent Tool"
description: "Convert the Google Calendar node into an agent tool that the model can query dynamically."
slug: "04-06-integrating-google-calendar"
course: "Build AI Agents with n8n"
module: "Capstone Project: Executive AI Assistant"
lessonNumber: 19
duration: "PT15M"
difficulty: "medium"
objectives:
  - "Attach a Tool node to the AI Agent Tools input port"
  - "Configure Google Calendar OAuth2 credentials"
  - "Define tool descriptions so the LLM knows when and how to query the calendar"
lastUpdated: '2026-09-15'
tags: ["google-calendar", "tools", "oauth2", "api"]
locale: 'en'
---

# Integrating Google Calendar as an Agent Tool

To grant the agent calendar access, we attach a **Google Calendar Tool** to the agent orange Tool port.

## Adding the Tool Node

1. Drag from the AI Agent node **Tools** connector.
2. Search for **Google Calendar** and select it as a **Tool**.
3. **Tool Name:** `google_calendar_tool`
4. **Tool Description:**
   ```text
   Use this tool to retrieve the user events and meetings. Specify timeMin and timeMax to define the search window.
   ```

## Configuring Calendar Operations

- **Operation:** `Get Many` (Events)
- **Calendar:** `Primary`
- **Fields:** Summary, Start, End, Location, Attendees, Description.

When the agent reaches Step 2 of its reasoning loop, it will format arguments matching this tool schema and execute the query automatically.
